# NYC Taxi 数据智能分析助手（Agent）开发历程分析报告

**报告日期**：2026 年 5 月 16 日
**模块名称**：NYC Taxi ADS Analyst（纽约出租车数据分析智能体）
**最终版本**：1233604（Schema 1\.2 修正版）

## 1\. 项目背景与目标

该智能体模块的核心任务是将用户自然语言查询转换为可执行的分析配置（初期为 DSL，后期统一为 SQL \+ 图表配置），服务于纽约出租车数据集的商业分析。系统严格遵循白名单、表结构约束和输出格式规范，确保安全、可执行且用户友好。

## 2\. 开发演进历程（主要阶段）

### 阶段 1：基础 DSL 模式（24 天前）

- **核心功能**：将自然语言映射为固定 JSON DSL。

- **关键约束**：

    - 支持指标（metric）：revenue, order\_cnt, passenger\_cnt 等 6 项。

    - 时间范围：1d/7d/30d/today/yesterday。

    - 维度与 filters 白名单严格限制。

- **输出**：纯 JSON（metric/time\_range/dimension/filters/chart）。

- **亮点**：6/6 测试场景全部通过，防御性强（非相关查询返回 `parse\_failed`）。

- **版本**：a2f3f23

### 阶段 2：双优先级模式升级（2 天前）

- **重大改进**：引入智能路由。

    - **优先级 1**：简单查询 → DSL JSON。

    - **优先级 2**：复杂查询（TOPN、对比、跨区、机场、分布等）→ SQL \+ `chart\_config`。

- **新增内容**：

    - 嵌入完整 18 张 ADS 表结构。

    - `chart\_config` 规范（支持 line/bar/pie/stacked\_bar/dual\_axis 等）。

    - Few\-shot 示例增强。

- **输出要求**：增加 `\&lt;\&lt;\&lt;START\&gt;\&gt;\&gt;` / `\&lt;\&lt;\&lt;END\&gt;\&gt;\&gt;` 包裹标记 \+ 单行 JSON 规则。

- **测试结果**：11/11 通过。

- **版本**：9e80a19 → ac34977

### 阶段 3：统一 SQL\+chart\_config 模式（2 天前）

- **决策变更**：移除 DSL 模式，**统一输出 SQL \+ explanation \+ chart\_config**。

- **原因**：简化下游处理，提升复杂查询覆盖率。

- **强化规则**：

    - 输出格式硬性要求置于最高优先级。

    - 明确禁止输出 DSL。

- **测试结果**：10/10 通过。

- **版本**：43fa11a

### 阶段 4：表结构修正与最终优化（1 天前）

- **关键修复**（基于 ADS\_Table\\\[\_Schema1\.2\.md\]\(\_Schema1\.2\.md\)）：

    - `analysis\_weekday\_analysis`：移除 `is\_weekend`，改用 `CASE WHEN day\_of\_week IN \(6,7\)`。

    - 热点表：`pu\_zone/do\_zone` → `zone\_name`。

    - `analysis\_kpi\_daily`：移除 `yellow\_trips/green\_trips`，统一用 `total\_trips`。

    - 数据时间范围严格限定为 **2025\-01\-01 至 2025\-03\-31**。

- **Few\-shot 示例全面同步更新**。

- **测试结果**：10/10 通过（含边界场景：超出日期范围返回 `parse\_failed`）。

- **最终版本**：1233604

## 3\. 技术架构与设计要点

- **实现方式**：纯 Prompt 驱动（无工具调用），依赖 `doubao\-seed\-2\-0\-lite\-260215` 模型。

- **核心文件**：

    - `config/agent\_llm\_config\.json`（系统提示词 \+ Schema \+ Few\-shot \+ 规则）。

    - `src/agents/agent\.py`（核心执行逻辑，短期记忆滑动窗口）。

- **防御机制**：严格白名单、非出租车 / 预测类查询一律 `parse\_failed`。

- **输出标准化**：`\&lt;\&lt;\&lt;START\&gt;\&gt;\&gt;` \+ 单行 JSON \+ `\&lt;\&lt;\&lt;END\&gt;\&gt;\&gt;`。

## 4\. 测试覆盖与质量保障

- **累计测试**：超过 30\+ 场景（简单、复杂、边界、错误查询）。

- **通过率**：各阶段均 100% 通过。

- **代表性测试**：

    - 趋势分析（line）

    - TOPN 排名（bar）

    - 对比分析（line \+ group\_field）

    - 占比分布（pie）

    - 机场 / 跨区流量（dual\_axis /line）

    - 周末判断、热点 zone\_name 使用

    - 日期范围越界处理

## 5\. 分析总结与经验教训

### 成功经验

1. **迭代式演进**：从简单 DSL 到复杂 SQL 的逐步升级路径清晰合理。

2. **Prompt 工程**：通过嵌入完整 Schema、Few\-shot、硬性规则和修正提醒，显著提升模型遵守度。

3. **输出标准化**：包裹标记 \+ 单行 JSON 极大方便下游解析。

4. **防御性设计**：有效防止幻觉和无效查询。

### 改进点

- 早期 DSL 与 SQL 双模式并存增加了复杂度，后期统一为单一模式是正确决策。

- 表结构变更频繁，建议后续引入 Schema 版本管理与自动校验。

- 数据时间范围限制需在提示词中更醒目提示用户。

### 当前状态评估

该 Agent 已达到**生产可用**水平，能够稳定处理纽约出租车数据的日常分析查询，支持从简单趋势到复杂商业洞察的全场景覆盖，输出高度结构化且可直接执行。

---

**报告结束**
*开发历程完整记录于项目内部提交历史（a2f3f23 → 1233604）。*

> （注：文档部分内容可能由 AI 生成）
