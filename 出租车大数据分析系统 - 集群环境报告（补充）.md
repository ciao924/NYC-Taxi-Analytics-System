# 出租车大数据分析系统 \- 集群环境报告（补充）

# **补充日期**：2026\-04\-26
**补充内容**：DolphinScheduler 3\.1\.8 \+ Spark 3\.1\.3

---

## 十七、新增组件信息

### 17\.1 Spark 安装信息

|配置项|值|
|---|---|
|版本|3\.1\.3|
|安装路径|`/opt/module/spark\-3\.1\.3`|
|部署模式|YARN Client / Cluster|
|历史日志路径|`hdfs://hadoop102:8020/spark\-history`|
|事件日志目录|`hdfs://hadoop102:8020/spark\-history/events`|

**Spark 核心配置** \(`$SPARK\_HOME/conf/spark\-defaults\.conf`\)：

|配置项|值|
|---|---|
|`spark\.master`|`yarn`|
|`spark\.submit\.deployMode`|`cluster`|
|`spark\.sql\.warehouse\.dir`|`/user/hive/warehouse`|
|`spark\.sql\.catalogImplementation`|`hive`|
|`spark\.hadoop\.hive\.metastore\.uris`|`thrift://hadoop102:9083`|
|`spark\.serializer`|`org\.apache\.spark\.serializer\.KryoSerializer`|
|`spark\.sql\.adaptive\.enabled`|`true`|
|`spark\.sql\.adaptive\.coalescePartitions\.enabled`|`true`|
|`spark\.sql\.adaptive\.skewJoin\.enabled`|`true`|
|`spark\.dynamicAllocation\.enabled`|`true`|
|`spark\.dynamicAllocation\.maxExecutors`|`50`|
|`spark\.dynamicAllocation\.minExecutors`|`5`|

**Spark 环境变量** \(`/etc/profile`\)：

```bash
export SPARK_HOME=/opt/module/spark-3.1.3
export PATH=$PATH:$SPARK_HOME/bin:$SPARK_HOME/sbin
```

### 17\.2 DolphinScheduler 安装信息

|配置项|值|
|---|---|
|版本|3\.1\.8|
|安装路径|`/opt/module/dolphinscheduler\-3\.1\.8`|
|部署用户|`dolphin`|
|Web UI 端口|`12345`|
|默认管理员账号|`admin` / `dolphinscheduler123`|
|数据库类型|MySQL 8\.0|
|数据库名|`dolphinscheduler`|
|数据库用户|`dsuser`|
|Zookeeper 节点|`hadoop102:2181,hadoop103:2181,hadoop104:2181`|

### 17\.3 服务节点分配

|节点|DolphinScheduler 角色|Spark 角色|
|---|---|---|
|hadoop102|Master Server \+ API Server \+ Alert Server|Spark 客户端（提交作业）|
|hadoop103|Worker Server（可标记为 `realtime` 组）|\-|
|hadoop104|Worker Server（可标记为 `offline` 组）|\-|

### 17\.4 DolphinScheduler 核心配置

**数据库连接信息** \(`api\-server/conf/application\.yaml`\)：

```yaml
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://192.168.127.102:3306/dolphinscheduler?useUnicode=true&characterEncoding=utf8&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
    username: dsuser
    password: Ds@123456
```

**环境变量配置** \(`bin/env/dolphinscheduler\_env\.sh`\)：

```bash
export HADOOP_HOME=/opt/module/hadoop-3.3.1
export HADOOP_CONF_DIR=/opt/module/hadoop-3.3.1/etc/hadoop
export SPARK_HOME=/opt/module/spark-3.1.3
export FLINK_HOME=/opt/module/flink-1.17.2
export HIVE_HOME=/opt/module/hive-3.1.3
export PATH=$PATH:$HADOOP_HOME/bin:$SPARK_HOME/bin:$FLINK_HOME/bin:$HIVE_HOME/bin
```

**安装节点配置** \(`bin/env/install\_env\.sh`\)：

```bash
ips="hadoop102,hadoop103,hadoop104"
sshPort="22"
masters="hadoop102"
workers="hadoop103:default,hadoop104:default"
alertServer="hadoop102"
apiServers="hadoop102"
installPath="/opt/module/dolphinscheduler-3.1.8"
deployUser="dolphin"
javaHome="/opt/module/jdk1.8.0_431"
```

### 17\.5 DolphinScheduler 服务状态

|服务|节点|状态|端口|
|---|---|---|---|
|MasterServer|hadoop102|✅ RUNNING|\-|
|WorkerServer|hadoop103|✅ RUNNING|\-|
|WorkerServer|hadoop104|✅ RUNNING|\-|
|AlertServer|hadoop102|✅ RUNNING|\-|
|ApiApplicationServer|hadoop102|✅ RUNNING|12345|

### 17\.6 MySQL（DolphinScheduler 元数据库）

|配置项|值|
|---|---|
|主机|\[192\.168\.127\.102\]\(192\.168\.127\.102\)|
|端口|3306|
|数据库|dolphinscheduler|
|用户|dsuser|
|密码|Ds@123456|
|表数量|约 80\+ 张|

---

## 十八、JDBC 驱动配置

|组件|驱动文件|存放路径|
|---|---|---|
|MySQL 驱动|mysql\-connector\-java\-8\.0\.30\.jar|`/opt/software/`|
|DS MySQL 驱动|mysql\-connector\-java\-8\.0\.30\.jar|`/opt/module/dolphinscheduler\-3\.1\.8/libs/`（各模块）|

---

## 十九、DolphinScheduler 租户配置

|配置项|值|说明|
|---|---|---|
|租户编码|`dolphin`|对应 Linux 系统用户|
|租户名称|`dolphin`|\-|
|队列|`default`|YARN 队列|
|租户类型|普通用户|\-|

### 19\.1 Worker 分组（建议配置）

|分组名称|节点|用途|
|---|---|---|
|`realtime`|hadoop103|实时任务调度|
|`offline`|hadoop104|离线任务调度|

---

## 二十、Spark ETL 作业配置

### 20\.1 作业 JAR 包

|配置项|值|
|---|---|
|JAR 路径|`/opt/module/taxi\-etl/taxi\-etl\-4\.0\-SNAPSHOT\-jar\-with\-dependencies\.jar`|
|主类示例|`com\.taxi\.etl\.ods\.YellowOdsLoader`|
|配置文件|`application\-dev\.conf`（打包在 JAR 内或独立路径）|

### 20\.2 Spark 作业提交参数（DS 推荐配置）

|参数|值|说明|
|---|---|---|
|`\-\-master`|`yarn`|使用 YARN 资源管理|
|`\-\-deploy\-mode`|`cluster`|集群模式运行|
|`\-\-driver\-memory`|`4g`|Driver 内存|
|`\-\-executor\-memory`|`8g`|Executor 内存|
|`\-\-executor\-cores`|`4`|每 Executor 核心数|
|`\-\-num\-executors`|`4`|Executor 数量|
|`\-\-conf spark\.sql\.adaptive\.enabled`|`true`|启用 AQE|
|`\-\-conf spark\.sql\.adaptive\.coalescePartitions\.enabled`|`true`|动态分区合并|

### 20\.3 DS Spark 任务配置（界面填写指引）

在 DolphinScheduler 中创建 Spark 任务节点时，按如下方式配置：

**程序类型**：`Java/Scala`
**主 jar 包**：`/opt/module/taxi\-etl/taxi\-etl\-4\.0\-SNAPSHOT\-jar\-with\-dependencies\.jar`
**主类**：`com\.taxi\.etl\.ods\.YellowOdsLoader`（根据实际作业选择）
**部署方式**：`cluster`
**Driver 核心数**：`2`
**Driver 内存**：`4g`
**Executor 数量**：`4`
**Executor 核心数**：`4`
**Executor 内存**：`8g`

**任务参数（可选）**：

```Plain Text
2025-01-01 2025-03-31
```

---

## 二十一、推荐工作流依赖关系

```Plain Text
┌─────────────────────────────────────────────────────────────────┐
│                    DolphinScheduler 离线调度工作流               │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐                                          │
│  │  YellowOdsLoader │ (黄表 ODS 加载)                           │
│  └────────┬─────────┘                                          │
│           │                                                    │
│  ┌────────┴─────────┐                                          │
│  │  GreenOdsLoader  │ (绿表 ODS 加载)                           │
│  └────────┬─────────┘                                          │
│           │                                                    │
│           ▼                                                    │
│  ┌──────────────────┐                                          │
│  │   OdsValidator   │ (ODS 数据验证，可选)                      │
│  └────────┬─────────┘                                          │
│           │                                                    │
│           ▼                                                    │
│  ┌──────────────────┐                                          │
│  │  DwdLayerBuilder │ (DWD 层构建)                              │
│  └────────┬─────────┘                                          │
│           │                                                    │
│           ▼                                                    │
│  ┌──────────────────┐                                          │
│  │  DwsLayerBuilder │ (DWS 层构建)                              │
│  └────────┬─────────┘                                          │
│           │                                                    │
│           ▼                                                    │
│  ┌──────────────────┐                                          │
│  │  AdsLayerBuilder │ (ADS 层构建，可选多表并行)                │
│  └──────────────────┘                                          │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

---

## 二十二、端口汇总（补充）

|新增服务|端口|节点|说明|
|---|---|---|---|
|DolphinScheduler API|12345|hadoop102|DS Web UI 访问端口|
|Spark History Server|18080|hadoop102（可选）|Spark 历史事件查看|

---

## 二十三、快速运维命令（补充）

### 23\.1 DolphinScheduler

```bash
# 启动所有 DS 服务
sh /opt/module/dolphinscheduler-3.1.8/bin/start-all.sh

# 停止所有 DS 服务
sh /opt/module/dolphinscheduler-3.1.8/bin/stop-all.sh

# 查看 DS 服务状态
sh /opt/module/dolphinscheduler-3.1.8/bin/status-all.sh

# 查看各模块日志
tail -f /opt/module/dolphinscheduler-3.1.8/api-server/logs/dolphinscheduler-api.log
tail -f /opt/module/dolphinscheduler-3.1.8/master-server/logs/dolphinscheduler-master.log
tail -f /opt/module/dolphinscheduler-3.1.8/worker-server/logs/dolphinscheduler-worker.log
```

### 23\.2 Spark

```bash
# 提交 Spark 作业（集群模式）
spark-submit --master yarn --deploy-mode cluster \
  --class com.taxi.etl.ods.YellowOdsLoader \
  /opt/module/taxi-etl/taxi-etl-4.0-SNAPSHOT-jar-with-dependencies.jar

# 查看 Spark 历史作业
# 浏览器访问: http://hadoop102:18080

# 查看 YARN 应用列表
yarn application -list

# 查看特定 YARN 应用日志
yarn logs -applicationId application_xxx
```

---

## 二十四、环境验证清单（补充）

|验证项|命令|期望结果|
|---|---|---|
|Spark 可用性|`spark\-submit \-\-version`|显示版本 3\.1\.3|
|Spark 连接 Hive|`spark\-sql \-e \&\#34;show databases\&\#34;`|显示 nyc\_taxi\_ods 等数据库|
|DS 服务状态|`sh /opt/module/dolphinscheduler\-3\.1\.8/bin/status\-all\.sh`|所有服务 RUNNING|
|DS Web UI 访问|浏览器访问 `http://hadoop102:12345/dolphinscheduler`|显示登录页面|
|DS 租户配置|DS Web UI → 安全中心 → 租户管理|存在 `dolphin` 租户|

---

**补充报告结束**

*本补充由系统生成，最后更新时间：2026\-04\-26*

> （注：文档部分内容可能由 AI 生成）
