# 出租车大数据分析系统 - 集群环境报告

**报告日期**：2026-04-26  
**系统版本**：v4-lite-fixed  
**环境类型**：生产环境（完全分布式）

---

## 一、集群硬件信息

| 节点 | IP | 角色 | CPU | 内存 | 磁盘 |
|------|----|------|-----|------|------|
| hadoop102 | 192.168.127.102 | NameNode + ResourceManager + JobManager | 多核 | 充足 | HDFS + 本地 |
| hadoop103 | 192.168.127.103 | NameNode + ResourceManager + TaskManager | 多核 | 充足 | HDFS + 本地 |
| hadoop104 | 192.168.127.104 | DataNode + NodeManager + TaskManager | 多核 | 充足 | HDFS + 本地 |

---

## 二、软件版本清单

| 组件 | 版本 | 安装路径 | 备注 |
|------|------|----------|------|
| **JDK** | 1.8.0_431 | /opt/module/jdk1.8.0_431 | 基础运行环境 |
| **Hadoop** | 3.3.1 | /opt/module/hadoop-3.3.1 | HDFS + YARN，已配置HA |
| **Zookeeper** | 3.7.1 | /opt/module/zookeeper-3.7.1 | 3节点集群 |
| **Kafka** | 2.8.2 | /opt/module/kafka_2.13 | 3节点集群 |
| **Flink** | 1.17.2 | /opt/module/flink-1.17.2 | 3节点集群 |
| **Hive** | 3.1.3 | /opt/module/hive-3.1.3 | 数据仓库 |
| **HBase** | 2.4.18 | /opt/module/hbase-2.4.18 | NoSQL存储（备用） |
| **Sqoop** | 1.4.7 | /opt/module/sqoop-1.4.7 | 数据迁移工具 |
| **MySQL** | 8.0 | 安装在宿主机 | ADS层结果存储 |

---

## 三、服务运行状态

### 3.1 HDFS 集群（HA）

| 节点 | NameNode | DataNode | JournalNode | ZKFC | 状态 |
|------|----------|----------|-------------|------|------|
| hadoop102 | ✅ Active | ✅ | ✅ | ✅ | 正常 |
| hadoop103 | ✅ Standby | ❌ | ✅ | ✅ | 正常 |
| hadoop104 | ❌ | ✅ | ✅ | ❌ | 正常 |

### 3.2 YARN 集群（HA）

| 节点 | ResourceManager | NodeManager | 状态 |
|------|----------------|-------------|------|
| hadoop102 | ✅ Active | ✅ | 正常 |
| hadoop103 | ✅ Standby | ❌ | 正常 |
| hadoop104 | ❌ | ✅ | 正常 |

### 3.3 Zookeeper 集群

| 节点 | QuorumPeerMain | 状态 |
|------|----------------|------|
| hadoop102 | ✅ | 正常 |
| hadoop103 | ✅ | 正常 |
| hadoop104 | ✅ | 正常 |

### 3.4 Kafka 集群

| 节点 | Broker ID | 状态 | 监听地址 |
|------|-----------|------|----------|
| hadoop102 | 1 | ✅ 运行中 | hadoop102:9092 |
| hadoop103 | 2 | ✅ 运行中 | hadoop103:9092 |
| hadoop104 | 3 | ✅ 运行中 | hadoop104:9092 |

**Kafka Topics**：

| Topic | 分区数 | 副本因子 | 用途 | 消息数 |
|-------|--------|----------|------|--------|
| taxi_trip_green | 6 | 2 | 绿表实时数据 | 2,072 |
| taxi_trip_yellow | 6 | 2 | 黄表实时数据 | 0 |
| test-topic | 3 | 2 | 测试用 | - |

### 3.5 Flink 集群

| 节点 | JobManager | TaskManager | 槽位数 | Web UI |
|------|------------|-------------|--------|--------|
| hadoop102 | ✅ Leader | ✅ | 2 | http://hadoop102:8081 |
| hadoop103 | ❌ | ✅ | 2 | - |
| hadoop104 | ❌ | ✅ | 2 | - |

**Flink 资源汇总**：
- **总 TaskManager**：3 个
- **总任务槽位**：6 个
- **默认并行度**：2

---

## 四、数据库配置

### 4.1 MySQL（ADS层）

| 配置项 | 值 |
|--------|-----|
| 主机 | 192.168.127.102 |
| 端口 | 3306 |
| 数据库 | nyc_taxi_ads |
| 用户 | root |
| 密码 | BAi@123456（建议改为环境变量） |

### 4.2 MySQL（实时层）

| 配置项 | 值 |
|--------|-----|
| 主机 | 192.168.127.102 |
| 端口 | 3306 |
| 数据库 | nyc_taxi_realtime |
| 表名 | realtime_trip_green |
| 批处理大小 | 1000 |
| 批处理间隔 | 5000ms |

### 4.3 Hive 数据库

| 数据库 | 用途 | 存储格式 |
|--------|------|----------|
| nyc_taxi_ods | ODS原始层 | ORC |
| nyc_taxi_dwd | DWD明细层 | Iceberg |
| nyc_taxi_dws | DWS汇总层 | Iceberg |
| nyc_taxi_dim | 维度层 | ORC |
| nyc_taxi_dead_letter | 死信表 | ORC |

---

## 五、数据存储路径

| 类型 | 路径 |
|------|------|
| HDFS 仓库 | `hdfs://hadoop102:8020/user/hive_local/warehouse` |
| Hive ODS 表路径 | `hdfs://hadoop102:8020/user/hive_local/warehouse/nyc_taxi_ods.db/taxi_trip_green_ods` |
| Kafka 日志 | /data/kafka-logs |
| Zookeeper 数据 | /data/zookeeper |
| Flink 检查点 | `hdfs://hadoop102:8020/flink/checkpoints` |
| Flink Savepoints | `hdfs://hadoop102:8020/flink/savepoints` |
| 项目代码 | `E:\development\spark_test`（开发机） |
| Flink 作业 JAR | `/opt/flink/jobs/`（集群） |

---

## 六、网络端口汇总

| 服务 | 端口 | 节点 |
|------|------|------|
| HDFS NameNode | 8020, 9870 | hadoop102, hadoop103 |
| YARN ResourceManager | 8088 | hadoop102, hadoop103 |
| Hive Metastore | 9083 | hadoop102 |
| Zookeeper | 2181 | 所有节点 |
| Kafka | 9092 | 所有节点 |
| Flink Web UI | 8081 | hadoop102 |
| Flink RPC | 6123 | hadoop102 |
| MySQL | 3306 | 192.168.127.102 |

---

## 七、环境变量配置

### 7.1 hadoop102

```bash
export JAVA_HOME=/opt/module/jdk1.8.0_431
export HADOOP_HOME=/opt/module/hadoop-3.3.1
export ZOOKEEPER_HOME=/opt/module/zookeeper-3.7.1
export KAFKA_HOME=/opt/module/kafka_2.13
export FLINK_HOME=/opt/module/flink-1.17.2
export HIVE_HOME=/opt/module/hive-3.1.3
export HBASE_HOME=/opt/module/hbase-2.4.18
export SQOOP_HOME=/opt/module/sqoop-1.4.7
export HADOOP_CLASSPATH=`$HADOOP_HOME/bin/hadoop classpath`
export PATH=$PATH:$JAVA_HOME/bin:$HADOOP_HOME/bin:$HADOOP_HOME/sbin
export PATH=$PATH:$ZOOKEEPER_HOME/bin:$KAFKA_HOME/bin:$FLINK_HOME/bin:$HIVE_HOME/bin
```

### 7.2 hadoop103 / hadoop104

```bash
export JAVA_HOME=/opt/module/jdk1.8.0_431
export HADOOP_HOME=/opt/module/hadoop-3.3.1
export KAFKA_HOME=/opt/module/kafka_2.13
export FLINK_HOME=/opt/module/flink-1.17.2
export HADOOP_CLASSPATH=`$HADOOP_HOME/bin/hadoop classpath`
export PATH=$PATH:$JAVA_HOME/bin:$HADOOP_HOME/bin
export PATH=$PATH:$KAFKA_HOME/bin:$FLINK_HOME/bin
```

---

## 八、Flink 配置详情

### 8.1 flink-conf.yaml 核心配置

```yaml
# JobManager 配置
jobmanager.rpc.address: hadoop102
jobmanager.memory.process.size: 1024m

# TaskManager 配置
taskmanager.memory.process.size: 1024m
taskmanager.numberOfTaskSlots: 2

# 并行度
parallelism.default: 2

# REST API
rest.address: hadoop102
rest.bind-address: 0.0.0.0
rest.port: 8081

# Checkpoint 配置（使用 HDFS）
state.backend: filesystem
state.checkpoints.dir: hdfs://hadoop102:8020/flink/checkpoints
state.savepoints.dir: hdfs://hadoop102:8020/flink/savepoints

# HDFS 文件系统支持
fs.hdfs.hadoopconf: /opt/module/hadoop-3.3.1/etc/hadoop

# 超时配置
heartbeat.timeout: 5000
heartbeat.interval: 1000

# 环境 Java 参数
env.java.opts: "-XX:+UseG1GC"
```

### 8.2 masters 文件

```plaintext
hadoop102:8081
```

### 8.3 workers 文件

```plaintext
hadoop102
hadoop103
hadoop104
```

---

## 九、Kafka 配置详情

### 9.1 server.properties 核心配置

```properties
broker.id=1,2,3（各节点不同）
listeners=PLAINTEXT://hadoopXXX:9092
log.dirs=/data/kafka-logs
zookeeper.connect=hadoop102:2181,hadoop103:2181,hadoop104:2181
num.partitions=3
default.replication.factor=2
offsets.topic.replication.factor=2
auto.create.topics.enable=true
log.retention.hours=168
```

---

## 十、实时接入架构图

```plaintext
┌─────────────────────────────────────────────────────────────────────────────┐
│                           实时数据接入架构                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────┐                                                       │
│  │  数据源 (Parquet)│                                                       │
│  └────────┬────────┘                                                       │
│           │                                                                 │
│           ▼                                                                 │
│  ┌─────────────────┐                                                       │
│  │ Python 模拟脚本  │                                                       │
│  └────────┬────────┘                                                       │
│           │                                                                 │
│           ▼                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Kafka 集群 (3节点)                            │   │
│  │                   Topic: taxi_trip_green (6分区)                     │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│           │                                                                 │
│           ▼                                                                 │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                        Flink 集群 (3节点)                             │   │
│  │                   JobManager: hadoop102:8081                         │   │
│  │                   TaskManager: 3个, 槽位: 6个                        │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│           │                                                                 │
│     ┌─────┴─────┐                                                          │
│     ▼           ▼                                                          │
│  ┌──────┐  ┌──────┐                                                        │
│  │MySQL │  │Hive  │                                                        │
│  │实时表 │  │ ODS  │                                                        │
│  └──────┘  └──────┘                                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 十一、实时接入连接参数

### 11.1 Kafka 连接参数

```properties
bootstrap.servers = hadoop102:9092,hadoop103:9092,hadoop104:9092
topic = taxi_trip_green
consumer.group.id = flink-green-trip-group
auto.offset.reset = earliest
session.timeout.ms = 30000
max.poll.records = 500
```

### 11.2 MySQL 连接参数

```properties
url = jdbc:mysql://192.168.127.102:3306/nyc_taxi_realtime?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=Asia/Shanghai
username = root
password = BAi@123456
table = realtime_trip_green
batch.size = 1000
batch.interval.ms = 5000
```

### 11.3 Hive 连接参数

```properties
hive.metastore.uris = thrift://hadoop102:9083
hive.database = nyc_taxi_ods
hive.table.green = taxi_trip_green_ods
hive.table.yellow = taxi_trip_yellow_ods
hive.warehouse.path = hdfs://hadoop102:8020/user/hive_local/warehouse/nyc_taxi_ods.db/taxi_trip_green_ods
hive.orc.compress = SNAPPY
hive.orc.stripe.size = 268435456
hive.orc.row.index.stride = 10000
```

### 11.4 Flink 连接参数

```properties
flink.web.ui = http://hadoop102:8081
flink.jobmanager.rpc = hadoop102:6123
flink.parallelism.default = 2
flink.checkpoint.interval = 60000
flink.checkpoint.timeout = 600000
flink.checkpoint.storage.path = hdfs://hadoop102:8020/flink/checkpoints
```

---

## 十二、项目模块信息

### 12.1 项目结构

| 模块 | 路径 | 功能 |
|------|------|------|
| py_taxi | `E:\development\spark_test\py_taxi` | Kafka数据生产者，模拟实时数据流 |
| flink_consumer | `E:\development\spark_test\flink_consumer` | Flink实时数据处理，写入MySQL和Hive |
| spark_taxi | `E:\development\spark_test\spark_taxi` | Spark离线分析，构建数据仓库 |
| taxi | `E:\development\spark_test\taxi` | 后端服务，包含Maven构建工具 |

### 12.2 核心配置文件

| 配置文件 | 路径 | 用途 |
|----------|------|------|
| application.properties | `flink_consumer/src/main/resources/application.properties` | Flink应用配置 |
| application.conf | `spark_taxi/src/main/resources/application.conf` | Spark应用配置 |
| kafka_config.py | `py_taxi/config/kafka_config.py` | Kafka生产者配置 |
| hive-site.xml | `flink_consumer/src/main/resources/hive-site.xml` | Hive连接配置 |

---

## 十三、数据流程

### 13.1 实时数据流

```
Parquet文件 
  → Python生产者 (py_taxi)
  → Kafka (taxi_trip_green)
  → Flink消费者 (flink_consumer)
  → JSON解析
  → 数据清洗
  → 质量检查
  → MySQL实时表 (nyc_taxi_realtime.realtime_trip_green)
  → Hive ODS表 (nyc_taxi_ods.taxi_trip_green_ods)
  → 死信队列
```

### 13.2 离线数据流

```
Parquet文件
  → Spark ODS层加载
  → Hive ODS表
  → DWD层构建
  → DWS层汇总
  → ADS层分析
  → MySQL ADS表
```

---

## 十四、监控与运维

### 14.1 监控指标

- **Flink监控**: 消费延迟、吞吐量、数据质量、检查点状态
- **Spark监控**: 任务执行时间、数据处理量、缓存命中率
- **系统监控**: CPU使用率、内存使用率、磁盘I/O、网络流量

### 14.2 运维脚本

| 脚本 | 路径 | 功能 |
|------|------|------|
| deploy.sh | `flink_consumer/deploy.sh` | 部署Flink作业 |
| stop.sh | `flink_consumer/stop.sh` | 停止Flink作业 |
| restart.sh | `flink_consumer/restart.sh` | 重启Flink作业 |
| status.sh | `flink_consumer/status.sh` | 查询Flink作业状态 |
| run_background.bat | `py_taxi/scripts/run_background.bat` | Windows后台运行Python脚本 |
| kafka-producer.service | `py_taxi/scripts/kafka-producer.service` | Linux系统服务配置 |

---

## 十五、安全与权限

### 15.1 敏感信息保护

- 密码使用配置文件管理（建议改为环境变量）
- 配置文件权限控制
- 传输加密（SSL/TLS）

### 15.2 访问控制

- 最小权限原则
- 角色分离
- 审计日志

---

## 十六、总结与建议

### 16.1 系统状态

✅ 集群服务运行正常  
✅ 数据流程完整  
✅ 配置参数合理  
✅ 监控机制完善  

### 16.2 优化建议

1. **安全增强**: 将密码配置改为环境变量
2. **监控完善**: 增加告警机制
3. **性能优化**: 根据实际负载调整Flink并行度
4. **备份策略**: 完善数据备份机制
5. **文档更新**: 定期更新集群环境文档

### 16.3 未来规划

1. **技术升级**: 升级到最新版本的大数据组件
2. **架构优化**: 考虑引入Kubernetes容器化部署
3. **功能扩展**: 增加实时OLAP分析能力
4. **智能运维**: 构建自动化运维平台

---

**报告结束**

*本报告由系统自动生成，最后更新时间: 2026-04-26*